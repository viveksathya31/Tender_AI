# Phase 1 — Document Ingestion
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Document parsing
pdfplumber>=0.11.0
pdf2image>=1.17.0          # rasterise scanned PDFs (requires poppler system package)
python-docx>=1.1.0
Pillow>=10.3.0

# OCR
pytesseract>=0.3.13        # Tesseract wrapper (requires tesseract-ocr system package)
boto3>=1.34.0              # AWS Textract (optional — cloud fallback)
numpy>=1.26.0              # used in deskew and binarisation

# Data validation
pydantic>=2.7.0

# Testing
pytest>=8.2.0
httpx>=0.27.0              # for FastAPI test client

# ── System dependencies (install separately) ──────────────────────────────
# Ubuntu/Debian:
#   sudo apt-get install tesseract-ocr poppler-utils
# macOS:
#   brew install tesseract poppler